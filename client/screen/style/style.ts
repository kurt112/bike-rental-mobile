import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    bikeContainer: {
        paddingTop: '5%',
        flex: 1,
        marginBottom: 10
    },
    fonts: {
        marginBottom: 8,
    }
});

export default styles